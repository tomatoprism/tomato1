<% form_for %>

<p> attachment_image_tag prodect, :image_id </p>
<p> product.category_id </p>
<p> product.artist_id </p>
<p> product.title </p>
<p> product.label </p>
<p> product.release </p>
<p> product.price </p>
<p> product.stock </p>
<p> link_to "お気に入り", product_favorites_path, method: :post %>
<p>個数</p>
<%= f.number_field_tag 'quantity', nil, in: 1...100
# => <input id="quantity" name="quantity" min="1" max="9" type="number" /> %>
<%= link_to "カートに入れる", cart_path(cart.id) %>

<table class="table">
　　　　　<tr>
		    <th> 詳細 </th>
		    <th> 曲名一覧 </th>
		    <th> アーティスト </th>
		    <th> 商品説明 </th>
	     </tr>

	     <tr>
		    <th> </th>
		    <th> product.disc.track </th>
		    <th> product.artist_id </th>
		    <th> product.introduction </th>
	     </tr>
</table>


<h2>レビュー</h2>

<%= form_for @review do |f| %>
<%= f.text_field :text, rows:'5', class: "form-control", placeholder: "コメントをここに" %>
    <%= f.submit "送信する" %>
<% end %>

 <% product.product_id.review.each do |review| %>
 <%= post_comment.created_at.strftime('%Y/%m/%d') %>
 <%= post_comment.comment %>
 <% end %>

 <h3>関連情報</h3>

 <%= link_to "戻る", books_path, class: "back" %>
