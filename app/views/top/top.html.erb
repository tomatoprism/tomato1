<div class="container">
	<div class="row">
			<%= render 'category', categories: @categories, artists: @artists %>

		<div class="col-xs-9 ">
           <% @products.each do |product| %>
              <div class = "item top">
                 <div class = "product-image" >
                   <%= link_to product_path(product.id) do %>
                   <%= attachment_image_tag product, :image, fallback: "no_image.jpg", size: '200x200' %>
                   <% end %>
                 </div>


                   <div class = "product-info" >
                     <ul>
                       <li> <%= product.title %> </li>
                       <li>発売日<br>
      	                   <%= product.release %></li>
                       <li> <%= product.price %>円 </li>
                        <li><%= link_to "詳細", product_path(product) %></li>
                      </ul>
                   </div>
               </div>
            <% end %>
        </div>
        <div class="col-lg-10 ">
           <% @ranking.each do |ranking| %>
              <div class = "item top">
                 <div class = "ranking-image" >
                   <%= link_to product_path(ranking) do %>
                   <%= attachment_image_tag ranking, :image, fallback: "no_image.jpg", size: '200x200' %>
                   <% end %>
                 </div>


                   <div class = "ranking-info" >
                     <ul>
                       <li> <%= ranking.title %> </li>
                       <li>発売日<br>
                           <%= ranking.release %></li>
                       <li> <%= ranking.price %>円 </li>
                        <li><%= link_to "詳細", product_path(ranking) %></li>
                      </ul>
                   </div>
               </div>
            <% end %>
        </div>
    </div>
 </div>


